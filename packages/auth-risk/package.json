{
  "name": "@blindfold/risk",
  "version": "0.1.0",
  "type": "module",
  "description": "Adaptive/dynamic security for Blindfold Auth: risk scoring from device, IP, velocity and travel signals, with step-up MFA decisions recorded to risk_events.",
  "main": "dist/src/index.js",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "test": "node --test dist/test/*.test.js",
    "build": "tsc -p .",
    "pretest": "npm run build"
  },
  "peerDependencies": {
    "@blindfold/auth": "0.1.0"
  },
  "types": "dist/src/index.d.ts"
}
