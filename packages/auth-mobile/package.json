{
  "name": "@dmc--98/blindfold-mobile",
  "version": "1.0.0-rc.1",
  "type": "module",
  "description": "Mobile-friendly Blindfold Auth client: PKCE S256 helpers, pluggable secure token storage, login + rotating refresh + logout, ready to drop into React Native / Swift / Kotlin via HTTP.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p .",
    "pretest": "npm run build",
    "test": "node --test dist/test/*.test.js"
  },
  "peerDependencies": {
    "@dmc--98/blindfold-auth": "0.1.0"
  }
}
